<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Employee | Premium</title>

    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root {
            --primary-gold: #ffd700;
            --update-orange: #ff8c00;
            --bg-dark: #0a0e17;
            --card-bg: #161c2a;
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Outfit', sans-serif;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            padding: 20px;
        }

        .form-container {
            background: var(--card-bg);
            border: 1px solid rgba(255, 215, 0, 0.2);
            border-radius: 30px;
            padding: 50px;
            width: 100%;
            max-width: 650px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.6);
        }

        h1 {
            font-weight: 800;
            background: linear-gradient(to right, var(--update-orange), #fff, var(--primary-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 35px;
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .form-label {
            color: var(--primary-gold);
            font-weight: 600;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            margin-bottom: 8px;
            display: block;
        }

        /* Inputs & Select Styling */
        .form-control, .form-select {
            background-color: rgba(0, 0, 0, 0.3) !important;
            border: 1px solid rgba(255, 215, 0, 0.15) !important;
            color: #fff !important;
            border-radius: 12px;
            padding: 12px 15px;
            transition: all 0.3s ease;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--update-orange) !important;
            box-shadow: 0 0 15px rgba(255, 140, 0, 0.2) !important;
        }

        /* Remove Blue from Select */
        .form-select option {
            background-color: var(--card-bg);
            color: white;
        }

        /* Option Group for Radio/Checkbox */
        .option-group {
            background: rgba(255, 255, 255, 0.03);
            padding: 15px;
            border-radius: 12px;
            border: 1px solid rgba(255, 215, 0, 0.1);
            margin-bottom: 5px;
        }

        .form-check-input {
            border-color: var(--primary-gold);
            cursor: pointer;
        }

        .form-check-input:checked {
            background-color: var(--update-orange);
            border-color: var(--update-orange);
        }

        /* Update Button Design */
        .btn-update {
            background: linear-gradient(45deg, var(--update-orange), var(--primary-gold));
            color: #000 !important;
            font-weight: 700;
            width: 100%;
            padding: 15px;
            border-radius: 15px;
            border: none;
            margin-top: 25px;
            transition: 0.4s;
            text-transform: uppercase;
            letter-spacing: 2px;
            box-shadow: 0 8px 20px rgba(255, 140, 0, 0.2);
        }

        .btn-update:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(255, 140, 0, 0.4);
            filter: brightness(1.1);
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #777;
            text-decoration: none;
            font-size: 0.9rem;
            transition: 0.3s;
        }

        .back-link:hover {
            color: var(--primary-gold);
        }
    </style>
</head>

<body>

    <div class="form-container">
        <h1>Update Employee</h1>

        <form method="post" action="/employee/updateEmp">
            <input type="hidden" name="emp_id" value="<%= singleEmp['id'] %>">
            
            <div class="row">
                <div class="col-md-6 mb-4">
                    <label class="form-label">Full Name</label>
                    <input type="text" name="emp_name" value="<%= singleEmp['emp_name']%>" class="form-control">
                </div>
                <div class="col-md-6 mb-4">
                    <label class="form-label">Email Address</label>
                    <input type="email" name="emp_email" value="<%= singleEmp['emp_email']%>" class="form-control">
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label">Password</label>
                <input type="password" name="emp_password" value="<%= singleEmp['emp_password']%>" class="form-control">
            </div>

            <div class="mb-4">
                <label class="form-label">Gender</label>
                <div class="option-group">
                    <div class="form-check form-check-inline">
                        <input type="radio" name="emp_gender" value="Male" id="male" class="form-check-input" 
                        <% if(singleEmp['emp_gender']=='Male' ) { %> checked <% }%>>
                        <label class="form-check-label" for="male">Male</label>
                    </div>
                    <div class="form-check form-check-inline">
                        <input type="radio" name="emp_gender" value="Female" id="female" class="form-check-input" 
                        <% if(singleEmp['emp_gender']=='Female' ) { %> checked <% }%>>
                        <label class="form-check-label" for="female">Female</label>
                    </div>
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label">Salary (Monthly)</label>
                <input type="number" name="emp_salary" value="<%= singleEmp['emp_salary']%>" class="form-control">
            </div>

            <div class="mb-4">
                <label class="form-label">Hobbies</label>
                <div class="option-group">
                    <% ["Reading", "Music" , "Cricket" , "Uno (Game)" , "Other" ].map((hobby)=> { %>
                        <div class="form-check form-check-inline">
                            <input type="checkbox" name="emp_hobby" value="<%= hobby %>" id="<%= hobby %>"
                                <% if(singleEmp['emp_hobby'].includes(hobby)) {%> checked <%}%> class="form-check-input">
                            <label class="form-check-label" for="<%= hobby %>"><%= hobby %></label>
                        </div>
                    <% }) %>
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label">Role / Designation</label>
                <select class="form-select" name="emp_role">
                    <% ["Owner", "Manager" , "HR" , "Employee" ].map((role)=> {%>
                        <option value="<%= role %>" <%if(singleEmp['emp_role']==role) {%> selected <%}%>>
                            <%= role %>
                        </option>
                    <% }) %>
                </select>
            </div>

            <button type="submit" class="btn-update">Save Changes</button>
        </form>

        <a href="/employee/allEmployeePage" class="back-link">← Cancel and Go Back</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>