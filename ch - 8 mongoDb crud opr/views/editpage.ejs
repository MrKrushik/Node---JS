<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book - Library Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #eef2f5;
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .main-card {
            background: #fff;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            border: none;
            max-width: 900px;
            width: 100%;
            margin: 20px;
        }

        /* Left Side Design Area */
        .design-side {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            text-align: center;
        }

        .design-side h1 {
            font-size: 3.5rem;
            margin-bottom: 10px;
        }

        .design-side h2 {
            font-weight: 700;
            margin-bottom: 15px;
        }

        .design-side p {
            font-size: 0.9rem;
            opacity: 0.8;
            max-width: 80%;
        }

        /* Right Side Form Area */
        .form-side {
            padding: 50px;
        }

        /* Active State Colors */
        .form-floating>.form-control:focus~label,
        .form-floating>.form-control:not(:placeholder-shown)~label,
        .form-floating>.form-select~label {
            color: #764ba2;
        }

        .form-control:focus, .form-select:focus {
            border-color: #764ba2;
            box-shadow: 0 0 0 0.25rem rgba(118, 75, 162, 0.25);
        }

        /* Update Button Styling (Orange Gradient) */
        .btn-update {
            background: linear-gradient(135deg, #FF8008 0%, #FFC837 100%);
            border: none;
            color: white;
            padding: 12px;
            font-weight: 600;
            border-radius: 10px;
            margin-top: 10px;
            transition: transform 0.2s;
        }

        .btn-update:hover {
            transform: scale(1.02);
            color: white;
            box-shadow: 0 5px 15px rgba(255, 128, 8, 0.3);
        }
        
        .back-link {
            text-align: center;
            display: block;
            margin-top: 20px;
            color: #777;
            text-decoration: none;
            font-size: 0.9rem;
        }
        .back-link:hover {
            color: #764ba2;
        }

        @media (max-width: 768px) {
            .design-side {
                padding: 30px;
            }
        }
    </style>
</head>

<body>

    <div class="main-card">
        <div class="row g-0">
            <div class="col-md-5 design-side">
                <h1>✏️</h1>
                <h2>Edit Details</h2>
                <p>Updating information ensures your library database stays accurate and up-to-date.</p>
            </div>

            <div class="col-md-7 form-side">
                <h3 class="mb-4 fw-bold text-secondary">Update Book</h3>

                <form method="post" action="/updateBook">
                    
                    <input type="hidden" name="id" value="<%= book.id %>">

                    <div class="form-floating mb-3">
                        <input type="text" name="book_name" class="form-control" id="fName" 
                               value="<%= book.book_name %>" placeholder="Book Name" required>
                        <label for="fName">Book Name</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input type="text" name="book_author" class="form-control" id="fAuthor" 
                               value="<%= book.book_author %>" placeholder="Author" required>
                        <label for="fAuthor">Author Name</label>
                    </div>

                    <div class="row g-2">
                        <div class="col-md-6">
                            <div class="form-floating mb-3">
                                <input type="number" name="book_price" class="form-control" id="fPrice" 
                                       value="<%= book.book_price %>" placeholder="Price" required>
                                <label for="fPrice">Price (₹)</label>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="form-floating mb-3">
                                <select class="form-select" name="book_lang" id="fLang" aria-label="Select Language" required>
                                    <option value="" disabled>Select Language</option>
                                    
                                    <option value="English" <%= book.book_lang === 'English' ? 'selected' : '' %>>English</option>
                                    <option value="Hindi" <%= book.book_lang === 'Hindi' ? 'selected' : '' %>>Hindi</option>
                                    <option value="Gujarati" <%= book.book_lang === 'Gujarati' ? 'selected' : '' %>>Gujarati</option>
                                    <option value="Marathi" <%= book.book_lang === 'Marathi' ? 'selected' : '' %>>Marathi</option>
                                    <option value="Sanskrit" <%= book.book_lang === 'Sanskrit' ? 'selected' : '' %>>Sanskrit</option>
                                    <option value="Other" <%= book.book_lang === 'Other' ? 'selected' : '' %>>Other</option>
                                
                                </select>
                                <label for="fLang">Language</label>
                            </div>
                        </div>
                    </div>

                    <div class="form-floating mb-4">
                        <input type="text" name="book_image" class="form-control" id="fImage" 
                               value="<%= book.book_image %>" placeholder="Image Link" required>
                        <label for="fImage">Image URL Link</label>
                    </div>

                    <button type="submit" class="btn btn-update w-100">Save Changes</button>
                    
                    <a href="/" class="back-link">Cancel & Go Back</a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>