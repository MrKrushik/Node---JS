<!DOCTYPE html>
<html lang="en">

<head>
    <title>Form Page</title>
       <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 50%, #1a1a1a 100%);
            min-height: 100vh;
            padding: 20px;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 20% 50%, rgba(255, 140, 0, 0.1) 0%, transparent 50%),
                        radial-gradient(circle at 80% 50%, rgba(192, 192, 192, 0.1) 0%, transparent 50%);
            pointer-events: none;
        }

        h1 {
            text-align: center;
            background: linear-gradient(135deg, #ff8c00 0%, #ffa500 50%, #c0c0c0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 2.5em;
            margin-bottom: 30px;
            text-shadow: 0 0 30px rgba(255, 140, 0, 0.3);
            letter-spacing: 3px;
            font-weight: bold;
            position: relative;
        }

        form {
            display: flex;
            justify-content: center;
            margin-bottom: 40px;
        }

        table {
            margin-top: 20px;
            border-collapse: collapse;
            background: linear-gradient(135deg, #2a2a2a 0%, #1f1f1f 100%);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6),
                        0 0 20px rgba(255, 140, 0, 0.2);
            border: 1px solid rgba(255, 140, 0, 0.3);
            position: relative;
        }

        form table {
            border: 1px solid rgba(192, 192, 192, 0.2);
        }

        form td {
            padding: 15px 20px;
            border: none;
        }

        form tr:nth-child(odd) td:first-child {
            background: linear-gradient(135deg, #ff8c00 0%, #ff6600 100%);
            color: white;
            font-weight: bold;
            width: 150px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }

        form tr:nth-child(even) td:first-child {
            /* background: linear-gradient(135deg, #c0c0c0 0%, #a8a8a8 100%);
            color: #1a1a1a; */
            font-weight: bold;
            width: 150px;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.3);
        }

        form tr td:last-child {
            background: #1a1a1a;
        }

        form input[type="text"],
        form input[type="email"],
        form input[type="password"],
        form input[type="number"],
        form textarea {
            width: 300px;
            padding: 12px;
            background: #2a2a2a;
            border: 2px solid #444;
            border-radius: 8px;
            font-size: 14px;
            transition: all 0.3s ease;
            outline: none;
            color: #e0e0e0;
        }

        form input[type="text"]:focus,
        form input[type="email"]:focus,
        form input[type="password"]:focus,
        form input[type="number"]:focus,
        form textarea:focus {
            border-color: #ff8c00;
            box-shadow: 0 0 15px rgba(255, 140, 0, 0.5),
                        inset 0 0 10px rgba(255, 140, 0, 0.1);
            transform: scale(1.02);
            background: #1f1f1f;
        }

        form textarea {
            resize: vertical;
            min-height: 80px;
        }

        form input[type="submit"] {
            background: linear-gradient(135deg, #ff8c00 0%, #ff6600 100%);
            color: white;
            padding: 12px 40px;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 140, 0, 0.4),
                        0 0 20px rgba(255, 140, 0, 0.2);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        form input[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 140, 0, 0.6),
                        0 0 30px rgba(255, 140, 0, 0.4);
            background: linear-gradient(135deg, #ffa500 0%, #ff8c00 100%);
        }

        form input[type="submit"]:active {
            transform: translateY(0);
        }

        table[align="center"][border="1"] {
            margin: 0 auto;
            border: 1px solid rgba(255, 140, 0, 0.3);
        }

        table[align="center"][border="1"] th,
        table[align="center"][border="1"] td {
            padding: 15px 20px;
            text-align: center;
            border: none;
            color: #e0e0e0;
        }

        table[align="center"][border="1"] tr:first-child {
            background: linear-gradient(135deg, #ff8c00 0%, #ff6600 50%, #c0c0c0 100%);
            color: white;
            font-weight: bold;
            font-size: 16px;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }

        table[align="center"][border="1"] tr:not(:first-child) {
            background: linear-gradient(135deg, #2a2a2a 0%, #1f1f1f 100%);
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(255, 140, 0, 0.1);
        }

        table[align="center"][border="1"] tr:not(:first-child):hover {
            /* background: linear-gradient(135deg, #ff8c00 0%, #ff6600 100%); */
            transform: scale(1.02);
            box-shadow: 0 5px 15px rgba(255, 140, 0, 0.4),
                        0 0 20px rgba(255, 140, 0, 0.3);
            color: white;
        }

        table[align="center"][border="1"] tr:nth-child(even):not(:first-child) {
            background: linear-gradient(135deg, #252525 0%, #1a1a1a 100%);
        }

        table[align="center"][border="1"] tr:nth-child(even):not(:first-child):hover {
            /* background: linear-gradient(135deg, #c0c0c0 0%, #a8a8a8 100%); */
            color: #1a1a1a;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes glow {
            0%, 100% {
                box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6),
                            0 0 20px rgba(255, 140, 0, 0.2);
            }
            50% {
                box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6),
                            0 0 30px rgba(255, 140, 0, 0.4);
            }
        }

        table {
            animation: fadeIn 0.6s ease, glow 3s ease-in-out infinite;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 1.8em;
            }

            form input[type="text"],
            form input[type="email"],
            form input[type="password"],
            form input[type="number"],
            form textarea {
                width: 200px;
            }

            table {
                font-size: 14px;
            }

            td, th {
                padding: 8px;
            }
        }
    </style>
</head>

<body>

    <h1 align="center">Table Page</h1>

    <table align="center" border="1">
        <tr align="center">
            <td>Id</td>
            <td>Name</td>
            <td>Email</td>
            <td>Password</td>
            <td>Phone no.</td>
            <td>Adress</td>
        </tr>

        <% data.map((data , index)=> {%>
            <tr>
                <td>
                    <%=data.id%>
                </td>
                <td>
                    <%=data.name%>
                </td>
                <td>
                    <%=data.email%>
                </td>
                <td>
                    <%=data.password%>
                </td>
                <td>
                    <%=data.phone%>
                </td>
                <td>
                    <%=data.adress%>
                </td>
            </tr>
            <% }) %>
    </table>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
</body>

</html>