<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Books - Library</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #eef2f5;
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            padding: 40px 0;
        }

        .main-card {
            background: #fff;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.05);
            padding: 30px;
            border: none;
        }

        .page-title {
            font-weight: 600;
            color: #333;
            margin-bottom: 0;
        }

        /* Gradient Button Style (Matching previous page) */
        .btn-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
            padding: 10px 25px;
            border-radius: 10px;
            font-weight: 500;
            transition: transform 0.2s;
        }

        .btn-gradient:hover {
            transform: translateY(-2px);
            color: white;
            box-shadow: 0 5px 15px rgba(118, 75, 162, 0.3);
        }

        /* Table Styling */
        .custom-table {
            border-collapse: separate;
            border-spacing: 0 15px;
            /* Rows ke beech space */
        }

        .custom-table thead th {
            border: none;
            background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: 500;
            padding: 15px;
            text-transform: uppercase;
            font-size: 0.85rem;
            letter-spacing: 1px;
        }

        .custom-table thead th:first-child {
            border-top-left-radius: 15px;
            border-bottom-left-radius: 15px;
        }

        .custom-table thead th:last-child {
            border-top-right-radius: 15px;
            border-bottom-right-radius: 15px;
        }

        .custom-table tbody tr {
            background-color: #fff;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.02);
            transition: transform 0.2s;
        }

        .custom-table tbody tr:hover {
            transform: scale(1.01);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            z-index: 2;
            position: relative;
        }

        .custom-table tbody td {
            border: none;
            vertical-align: middle;
            padding: 15px;
            color: #555;
            font-weight: 400;
        }

        .custom-table tbody td:first-child {
            border-top-left-radius: 10px;
            border-bottom-left-radius: 10px;
            font-weight: 600;
            color: #764ba2;
        }

        .custom-table tbody td:last-child {
            border-top-right-radius: 10px;
            border-bottom-right-radius: 10px;
        }

        /* Image Styling */
        .book-cover {
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            object-fit: cover;
            height: 100px;
            /* Fixed height for consistency */
            width: 70px;
        }

        /* Action Buttons */
        .action-btn {
            text-decoration: none;
            padding: 6px 15px;
            border-radius: 8px;
            font-size: 0.85rem;
            transition: all 0.2s;
        }

        .btn-edit {
            background-color: #eef2f5;
            color: #667eea;
            font-weight: 600;
        }

        .btn-edit:hover {
            background-color: #667eea;
            color: white;
        }

        .btn-delete {
            background-color: #fff0f0;
            color: #dc3545;
            font-weight: 600;
            margin-right: 5px;
        }

        .btn-delete:hover {
            background-color: #dc3545;
            color: white;
        }
    </style>
</head>

<body>

    <div class="container">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="page-title">📚 Library Collection</h2>
                <p class="text-muted">Manage your books effectively</p>
            </div>
            <a href="/addBookPage" class="btn btn-gradient">
                + Add New Book
            </a>
        </div>

        <div class="main-card">
            <div class="table-responsive">
                <table class="table custom-table">
                    <thead>
                        <tr>
                            <th scope="col" class="text-center">#</th>
                            <th scope="col">Book Name</th>
                            <th scope="col">Author</th>
                            <th scope="col">Price</th>
                            <th scope="col">Language</th>
                            <th scope="col">Cover</th>
                            <th scope="col" class="text-center">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% totalBooks.map((book, index)=> { %>
                            <tr>
                                <td scope="row" class="text-center">
                                    <%= index + 1 %>
                                </td>
                                <td class="fw-bold">
                                    <%= book.book_name %>
                                </td>
                                <td>
                                    <%= book.book_author %>
                                </td>
                                <td style="color: #28a745; font-weight: 600;">
                                    ₹<%= book.book_price %>
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border">
                                        <%= book.book_lang %>
                                    </span>
                                </td>
                                <td>
                                    <img src="<%= book.book_image %>" class="book-cover" alt="Book Image">
                                </td>

                                <td class="text-center">
                                    <a href="/deleteBook?bookId=<%= book.id %>" class="action-btn btn-delete">Delete</a>
                                    <a href="/editBook/<%= book.id %>" class="action-btn btn-edit">Edit</a>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>

            <% if (totalBooks.length===0) { %>
                <div class="text-center p-5">
                    <h4 class="text-muted">No books found in library.</h4>
                    <p>Click the button above to add your first book.</p>
                </div>
                <% } %>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>